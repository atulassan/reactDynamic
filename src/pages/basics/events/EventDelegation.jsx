import React from "react";

const EventDelegation = () => {
  return (
    <div>
      <h1>Event Delegation</h1>
      <p>Event delegation allows attaching a single event listener to a parent to handle events from its children.</p>
    </div>
  );
};

export default EventDelegation;
