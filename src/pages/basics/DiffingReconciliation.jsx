import React from "react";

const DiffingReconciliation = () => {
  return (
    <div>
      <h1>Diffing & Reconciliation</h1>
      <p>React updates only the parts of the DOM that have changed by using efficient diffing algorithms.</p>
    </div>
  );
};

export default DiffingReconciliation;
